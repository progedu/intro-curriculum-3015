doctype html
html(lang="ja")
  head
    meta(charset="UTF-8")
    title アンケート
  body
    h1 どれが食べたいですか？
    form(method="post" action=path)
      span 名前: 
      input(type="text" name="name")
      each item in items
        input(type="radio" name="favorite" value=item)
        span #{item}
      button(type="submit") 投稿
